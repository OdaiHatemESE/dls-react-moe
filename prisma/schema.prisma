

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlserver"
  url      = env("DATABASE_URL")
}

model testTable {
  Id Int @id(map: "PK_testTable") @default(autoincrement())
}
